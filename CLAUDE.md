# Zen Markdown 작업 규약

전역 규약(`~/.claude/CLAUDE.md`) 위에 얹는 이 저장소의 규약이다.

## 커밋과 푸시 때 버전별 빌드

2026-09-24 사용자 지정. 커밋(체크인)하고 푸시할 때 그 시점 버전의 확장 패키지(vsix)를 함께 빌드한다.

- **커밋할 때**: 그 커밋이 `package.json`의 `version`을 바꿨거나, 저장소 루트에 현재 버전의 `zen-markdown-<버전>.vsix`가 없으면 빌드한다.
- **푸시할 때**: 푸시 직전에 현재 버전의 vsix를 다시 빌드한다. 같은 버전 파일은 덮어써서, 푸시한 코드와 패키지가 같게 한다.
- **명령**: 저장소 루트에서 `npx @vscode/vsce package`를 돌린다. `vscode:prepublish`가 웹뷰 빌드와 호스트 컴파일을 함께 한다. 결과물은 저장소 루트의 `zen-markdown-<버전>.vsix`다.
- **실패하면**: 빌드가 실패하면 커밋하거나 푸시하지 않고 원인을 보고한다.
- **커밋하지 않는다**: vsix는 `.gitignore` 대상이다. 빌드 결과는 로컬 파일로만 남는다.
- **범위 밖**: git 태그와 GitHub 릴리스는 이 규약에 넣지 않는다. 요청이 있을 때만 한다. `.agents/AGENTS.md`의 "버전을 올리면 태그 푸시와 릴리스" 규칙은 따르지 않는다(푸시는 요청받을 때만 한다는 전역 규약과 충돌).

커밋 전 검사(웹뷰 build, lint, test, E2E와 루트 compile, test)는 그대로 먼저 돈다. vsix 빌드는 그 검사를 통과한 뒤에 한다.
